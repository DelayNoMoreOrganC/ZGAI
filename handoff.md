# ZGAI 并行开发交接

交接基线：本文件所在提交

更新时间：2026-07-24
主分支：`master`

## 1. 阅读顺序

每个开发 Agent 开始前必须依次阅读：

1. [README.md](README.md)：启动、配置、测试和安全边界。
2. [PRD.md](PRD.md)：业务规则、范围和验收标准。
3. 本文：当前进度、文件边界和合并要求。

旧阶段报告、轮次总结和旧 Agent 指令已清理，不能根据历史聊天或旧结论判断完成度；以当前代码、测试和这三份文档为准。

## 2. 当前运行基线

| 项目 | 状态 |
|---|---|
| 前端 | Vue 3/Vite，端口 `3017`，生产构建通过 |
| 后端 | Spring Boot 2.7/Java 11，端口 `8080`，291 项测试通过 |
| 归档 Worker | ZGAI 私有 Python 组件，端口仅容器内 `8091`，2 项生成测试通过 |
| 当前开发数据库 | H2 文件库 |
| 目标数据库 | PostgreSQL 16，profile、初始化脚本和离线迁移工具已存在 |
| 案件文件 | 本机/NAS；当前 NAS 约定挂载点 `/Volumes/ZGAI` |
| 知识文件 | NAS `knowledge-files` 或配置目录 |
| RAG | 未配置向量/模型时为 `KEYWORD / RETRIEVAL_ONLY` |
| Qdrant | 代码已接入，当前运行环境未稳定启用 |
| GD/AC/SSB | 统一按业务逻辑内化到 ZGAI；GD 民事归档 v1 已进入原生流程，AC/SSB 暂缓 |

已知非阻断警告：

- 历史 H2 同时存在空的 `WORK_REPORT` 与有数据的 `work_report`；迁移工具仅在确认重复表为空时忽略，若两个表都有数据会阻断迁移。
- 前端构建存在 Dart Sass legacy API 和大分块警告。
- 当前开发机没有 Docker Compose CLI；Compose 解析、镜像构建和 NAS 容器联调须在安装 Compose v2 的目标主机补验。
- 系统可用于局域网受控试用，不等于完成生产部署。

## 3. 每周开发进度

### 2026-07-13 至 2026-07-14

- 主界面开始收敛为案件、客户等核心模块。
- 新建客户取消“客户等级”，改为所属部门。
- 建立九个部门和身份类别基础。
- 电话校验兼容手机号和区号加固话。
- 新建案件取消案件等级，案由改为自由填写，收费方式统一为四类。
- 梳理承办人发起、行政审查、主任终审和一案一档规则。
- 形成首版研发进展和整体 PRD。

### 2026-07-15

- 导入员工档案与客户资料，员工账号按既定规则生成。
- 将“合伙人”身份调整为“部门主管”，识别部门主管。
- 案件列表按账号所属部门隔离。
- 客户列表按案源人或承办人的所属部门隔离。
- 利冲检查仍覆盖全客户库，不因此开放跨部门客户详情。

### 2026-07-16

- 开放财务管理与发票申请页面。
- 完成申请人修改、待审删除、财务查看、电子发票反馈和完成锁定规则。
- 调整员工权限数据。
- 规划 NAS 接入并配置 SMB 挂载方向。
- 确认文件库采用“数据库元数据 + NAS 文件本体”。

### 2026-07-17

- 开始案件文件模块，实现一案一档、目录、上传下载和元数据。
- 修复仲裁案件受理单位填写和真实承办人选择。
- 区分律师日历型工作台与行政审批待办型工作台。
- 案件筛选增加案由和部门。
- 审批中心升级为独立模块，详情改为右侧抽屉。
- 统一案件列表、消息中心和审批中心的审批资料展示。
- 支持审批同意、驳回理由和转审基础。

### 2026-07-18 至 2026-07-20

- 确定 PostgreSQL、NAS 文件库和 Qdrant 的目标数据架构。
- 明确案件文件默认不进入 RAG。
- 明确旧系统全量导入暂缓，先做基于现有案件要素的资料检索。
- 明确银行批量建案由 SSB 项目承接，ZGAI 先直接跳转。
- 完成多团队分工、外包范围和本地 AI 硬件决策框架。

### 2026-07-21

- 强化四类工作台和权限驱动菜单/路由。
- 收紧案件、客户、审批、文档、系统设置等高风险接口权限。
- 完成案件文件版本、回收站、分片上传和物理路径隐私保护。
- 完成知识文档 PDF/DOCX/TXT/MD 导入、原件下载和 RAG 降级检索。
- 增加系统健康中心、PostgreSQL 脚本、备份基础和角色冒烟脚本。
- 建立旧资料检索 MVP 与省时宝直接入口。
- 系统设置从演示数据改为真实用户、角色、审计、AI、备份和健康状态。

### 2026-07-22

- 旧资料检索改为必须选择当前有权案件，服务端提取强识别要素。
- 命中结果只暴露受控元数据，下载时重新校验案件权限和真实路径边界。
- 知识库增加来源依据、发布机关、文号、生效日期、有效状态和授权确认。
- 已废止法规、未授权外部参考资料禁止进入 RAG。
- 修复知识列表从第二页开始加载的问题，并支持在当前知识来源内搜索。
- 完成外部法律数据库初步调研：元典优先做 API/MCP 试点，北大法宝做专业对照；Alpha、法信、威科在取得书面接口与嵌入授权前只做外部跳转。
- 确定外部数据采用联邦检索和统一适配器，不抓取或复制商业数据库整库，不向供应商发送案件私密数据。
- 移除前端自动登录密码和固定 JWT/加密/数据库默认密钥，生产模式改为环境变量必填。
- 合并并清理 7 月前重复阶段报告，建立 README、PRD、handoff 三文档基线。
- 接入元典法规/案例语义检索与法律引证核验，增加独立知识库入口和管理员 API Key 配置。
- 元典检索结果采用人工逐条导入；法规按时效规则进入 RAG，案例默认作为未授权参考资料并禁止进入共享 RAG。
- 元典生成模型需邀请码，当前只作为法律数据底座，文本生成使用 LM Studio 或用户明确选择的云端模型。
- 完成 LM Studio 局域网接入，已验证 Qwen 模型发现、鉴权和原生 Chat API 非思考生成；本机密钥不进入 Git。
- 增加 LM Studio 单通道公平队列、600秒排队、502/503/504重试、300秒前端等待及 Java 连接中断时的安全兼容通道；模型机即使保留约10万上下文，也应保持并发1。
- 增加 ZGAI 律所基础系统提示词，并修复案件详情动态ID路由，避免进入字面量 `/case/:id/basic`。
- 新增案件 AI 独立工作台，案件详情与主工作台均可进入；SSB、AC 导航默认隐藏并由 `VITE_ENABLE_EXTERNAL_TOOLS` 控制。
- 新增案件 AI 指令审计、幂等、动作白名单和风险分级；明确开庭指令可建立日程、案件进展与 7 天/1 天/2 小时提醒，缺项不写入。
- 案件 AI 指令补充“今天/明天/后天/今晚、上午/下午/晚上、几点”等中文时间表达；无年份日期按最近的未来日期解析，明确的过去时间只追问不落库。
- 案件日程和待办新增对象级写权限约束：关联案件时必须具备案件编辑权限，仅有查看权限的行政或跨部门账号不能创建、改绑或修改案件事项；案件维度无权读取继续由服务端阻断。
- 三档开庭提醒只保存仍在未来的提醒节点，避免临近开庭时一次发送多条过期提醒；AI 结果面板同步显示实际写入的时间、地点、期限或阶段，便于律师当场核对。
- 新增结构化 `case_activity` 进展台账，以及指令、进展和日程之间的统一留痕。
- 新增智能归案临时区、SHA-256 查重、本地文字提取/Tesseract 中文 OCR、权限内候选案件和律师确认后 NAS 入档。
- 智能归案补齐关键日期确认闭环：识别到开庭时间/地点或期限时只展示建议，律师必须分别勾选并补全字段，才同步创建开庭日程、有效的三档提醒或个人期限待办；默认不勾选不会写入。
- 案件文件上传增加事务回滚清理：元数据或归案后的进展、日程、待办写入失败时，清理本次已复制到文件库的文件，避免孤立文件。
- 待归案临时区增加 72 小时生命周期和每小时分批清理：过期后删除临时原件并擦除 OCR 正文、分析结果和候选案件；清理严格限制在配置根目录内，异常越界路径标记 `CLEANUP_REJECTED` 且不删除。
- 确认归案的临时原件改为数据库事务提交成功后再删除；事务回滚时保留临时原件，同时案件目标文件由回滚补偿清理，避免两端文件同时丢失或留下孤立副本。
- PostgreSQL 基础脚本补齐 AI 指令、智能归案、案件进展、日程提醒和文档哈希结构；案件文档 OCR 正文改为大文本字段。

### 2026-07-23

- 确认正式系统采用 NAS 容器部署，NAS 承载业务、PostgreSQL、Qdrant、文档转换和文件归档，AI 推理由独立模型机承担。
- 增加 Mac 稳定 Demo 基础设施方案：PostgreSQL/Qdrant 使用本机 Docker 且仅绑定回环地址，案件和知识文件落 NAS，Windows 模型机承担生成与 Embedding；提供配置预检、启动、停止和回滚边界，现有 H2 不自动迁移。
- 核验模型机实际可用生成模型 `qwen/qwen3.6-35b-a3b` 与 Embedding 模型 `text-embedding-nomic-embed-text-v1.5`，Embedding 输出维度为 768；部署示例和 Qdrant collection 统一使用该维度。
- 增加腾讯云 Linux 稳定 Demo 替代方案：Mac 可继续开发，云端承载 ZGAI/PostgreSQL/Qdrant/LibreOffice，NAS 与模型机经 VPN 接入；明确公网禁止直接开放 SMB、LM Studio、PostgreSQL 和 Qdrant，云 Windows 暂不采用。
- 确认旧案需要导入，形成案件主表、当事人表、客户映射表和文件索引表组成的行政采集模板。
- 确认 AI 为本地优先、云端手动选择；计划接入 DeepSeek、GLM、Kimi，律所统一 Token 仅由后端安全保管。
- 已核验模型机实际生成模型 ID 为 `qwen/qwen3.6-35b-a3b`，Embedding 为 `text-embedding-nomic-embed-text-v1.5`、维度 768；量化等级和吞吐仍待正式压测。
- 明确 Word/Excel 使用 Apache POI 服务端生成，PDF 预览和转换使用 LibreOffice headless 容器，不把生产系统绑定到桌面 Office。
- 完成 NAS/AI/文档生成架构和官方 API 价格预算，硬件采购须在 NAS 设备盘点与模型基准后定型。
- 完成统一 AI 生成网关：默认固定 LM Studio，DeepSeek、GLM、Kimi 仅在当前会话明确选择后调用，不自动回退云端。
- AI 可用模型接口不返回密钥；新调用日志只保存 provider、模型、脱敏摘要、内容哈希、token、耗时、状态和估算费用，不再保存完整输入输出。
- 建立国家法律法规数据库链接预览、受控暂存、403 转人工上传，以及 NAS 制度只读扫描、哈希去重、确认复制和主任/知识管理员审核流程。
- 未审核制度、失效法规和禁止索引条目不能进入 RAG；扫描过程排除回收站、隐藏文件和 Office 临时文件，不修改 NAS 原件。
- 工作台、案件、客户、AI 和知识库入口完成首轮 macOS 浅色风格收敛；在 1440x900、1024x768、390x844 三个视口完成无页面级横向溢出检查。
- 客户详情补充关联案件、利冲与基于客户新建立案入口；案件创建可从客户上下文预填并继续要求人工确认核心信息。
- 利冲页面恢复为只输入客户名称；后端全库核对客户主体与案件当事人，支持相似名称和身份标识匹配，结果仅展示核查摘要，不开放无权详情。
- 每次利冲检查生成结构化命中依据、历史记录、唯一报告编号和可下载初筛报告，报告明确不替代行政正式结论。
- 利冲闭环增加行政正式审查：具有 `CASE_FILING_REVIEW` 权限的人员可作出通过、不通过或附条件通过结论；附条件通过强制填写豁免/风险处置依据，记录复核人和时间，提交后不可覆盖。
- 利冲留档报告同步包含系统初筛与人工正式结论；历史列表直接显示正式结论、复核人、时间和审查意见摘要，普通律师不能调用正式审查写接口。
- 局域网浏览器以普通账号“曾进朗”回归利冲页面：全库初筛、客户/案件当事人命中、历史报告和下载入口正常，未显示行政正式审查按钮，控制台无错误；行政/主任真实账号提交仍列入角色 E2E。
- 新建立案会在本案当事人写入前，对全部标记为“委托方”的主体生成案件专属利冲初筛，避免本案自身成为历史命中；若只关联客户库主体，也会以主客户补充生成。
- 审批详情抽屉直接展示案件全部委托方的初筛和正式结论，行政可在抽屉内完成正式审查；未全部审查或存在不通过结论时，前后端共同阻止“同意立案”。
- 主任终审通过后，结构化利冲记录自动生成案件报告写入 `01_立案材料`，建立 `case_document` 元数据并回填归档文档 ID/时间；案件基本信息同步展示利冲及归档状态。
- 附条件通过必须上传至少一份 PDF、Word、JPG 或 PNG 书面依据原件；文件先写入案件库受控暂存区并校验大小、SHA-256 和重复内容，正式结论锁定后不可追加。主任终审时原件事务性复制到 `01_立案材料`、生成 `CONFLICT_WAIVER` 案件文档，提交成功后清理暂存件，回滚时清理目标文件。
- 审批抽屉支持拖放上传和下载豁免原件，案件基本信息可查看并下载已关联附件；附件默认 `knowledgeEligible=false`、`indexStatus=FORBIDDEN`，不会进入全所 RAG。
- 客户详情新增“关联主体”标签页，可维护母子公司、关联企业、实际控制人、法定代表人、曾用名、担保关系等一层显式关系；既可选择当前有权客户，也可登记外部主体，反向关系只读，删除仅允许来源客户一侧执行。
- 利冲初筛新增 `RELATED_ENTITY / RELATION_GRAPH / RELATED` 命中类型；关联主体、关系方向和命中说明固化进检查记录与报告，后续关系变化不会抹去历史检查快照。该能力不自动开放关联客户详情。
- 利冲独立页面补齐案件专属附条件审查的原件上传，并禁止对无案件关联的普通预检作正式审批；客户详情日期格式缺失及旧渐变财务卡片同步修正。
- 存量待审批案件没有结构化利冲记录时保留旧审批意见路径，避免升级锁死；该兼容规则只适用于历史数据。
- 局域网运行态以普通律师账号“曾进朗”验证案件 9：案件基本信息已展示“立案利冲审查”，存量记录显示明确兼容提示；点击“查看审批”在右侧抽屉直接显示案件资料、审批路径和“0 个主体”的存量说明。行政正式审查按钮与主任终审归档仍需对应真实账号完成浏览器 E2E，当前由服务测试覆盖。
- 案件建案统一为民事诉讼、商事仲裁、刑事、行政、非诉专项和法律顾问六类，并为每类配置独立主体角色、办理阶段和详情要素。
- 商事仲裁从民事审理阶段拆分为独立案件类型；列表改为按案件类型展示主体关系，避免统一套用“原告 vs 被告”。
- 法律顾问建案必须选择客户库中的顾问单位，并可登记主联系人、服务期限、服务范围、响应要求、服务边界和续签提醒；顾问单位自动关联客户并写入案件主体。
- 顾问单位进一步限制为当前账号有权查看的非个人客户；案件类型从顾问切换到其他类型时会清理顾问专属字段和自动主体，后端也会忽略非顾问案件残留的顾问客户 ID。
- 新建案件及案件详情按六类案件展示标准流程和律师办理重点；详情中的机构、外部案号、程序、主体角色及专属字段不再统一套用民事案件标签。
- 前后端已统一商事仲裁和行政案件的阶段顺序；局域网普通律师账号“曾进朗”已实际验证顾问八步流程、顾问专属字段，以及商事仲裁十步流程、仲裁机构和仲裁案号展示。
- 案件详情的五个子页面改为路由页签，保留独立页面访问方式并消除 Element Plus 标签卸载报错；案件 9 已实际验证“基本案情 → 办案记录 → 离开详情”跳转正常且控制台无错误。
- 修复阶段待办模板使用中文类型而运行时使用代码导致无法匹配的问题；六类案件共同步 51 条系统阶段模板，待办在进入阶段时创建并分派给案件主办人。
- 移除案件详情前端重复生成阶段待办的旧链路，阶段推进和待办统一由后端处理；修复阶段回退误调用前进接口及跨阶段跳转提示。
- 增加立案审批中旧通用流程的保守迁移：仅无已完成历史的案件自动迁移，正式办理案件不改写历史。
- 案件列表筛选、详情“编辑团队”、案件动态提及及日程/待办表单已移除虚构人员，改为读取真实启用员工和有权案件；协办律师和助理现在允许清空。
- 立案流程修正为所有案件均执行“行政初审 → 主任终审”；固定收费等普通案件不再由行政初审直接建档，主任终审通过后才生成正式案号和案件文件夹。
- 审批、客户全库查看、开票处理和审批中案件修订已从姓名判断迁移为权限代码；新增 `CLIENT_AUDITOR`、`INVOICE_PROCESSOR`、`CASE_FILING_ADMIN` 三类能力角色，并保留环境变量形式的现有账号首次绑定。
- 客户全库查看使用 `CLIENT_VIEW_ALL`，立案路由使用 `CASE_FILING_REVIEW` / `CASE_FILING_FINAL_APPROVE`，开票处理使用 `INVOICE_PROCESS`；人员变化无需修改业务代码。
- 自动路由增加专职角色优先级，避免主任因全权限被选为行政初审或开票经办人；当前优先 `CASE_FILING_ADMIN`、`MANAGER`、`INVOICE_PROCESSOR`。
- 客户列表“详情”已统一跳转独立详情页，移除旧客户弹窗入口；普通律师账号已验证独立页面及“关联主体”标签可用。
- 后端自动测试基线更新为 160 项全部通过，覆盖两级审批、角色权限、六类案件流程、顾问单位边界、类型切换残留防护、阶段待办负责人、系统模板完整性、旧流程安全迁移、利冲全库/关联主体匹配、正式审查锁定、豁免原件校验、审批阻断、终审归档、案件日程/待办对象级权限、AI 中文时间解析、智能归案日期确认联动、文件写入失败清理、过期擦除、越界保护和提交后删除时序。
- 局域网普通律师账号“曾进朗”使用脱敏传票夹具完成智能归案浏览器验证：识别出文书类型、法院案号、法院、开庭时间和地点；“确认创建开庭日程”默认未勾选，勾选后正确预填 `2099-08-10 09:30` 与“第三审判庭”。测试未选择案件、未确认归案，因此未写入案件文件、日程或待办。
- 完成本地 OpenAI 兼容 Embedding 适配：本地服务无需云端 Token，不自动回退云端；查询与文档向量入口分离，支持批量、截断和维度校验。
- 修正 Qdrant collection 创建协议，增加实际维度检查和 `ready/incompatible/unavailable` 健康状态；系统设置可见 RAG、Embedding 和 Qdrant 状态，不展示密钥或内部地址。
- RAG 在未加载 Embedding 模型、Qdrant 离线或维度不一致时明确降级为关键词检索；补充生产构造器 Spring 注入回归，后端自动测试基线更新为 170 项，前端生产构建通过。
- 部署职责进一步明确：Windows 11 模型机（i9-14900KF、用户确认的 32GB 显存 GPU、64GB 内存）运行生成、Embedding 和后续重排；ZGAI、PostgreSQL、Qdrant 与文件默认常驻 NAS。只有 NAS 向量压测不达标时才把 Qdrant 迁到模型机本地 NVMe，并备份快照回 NAS。
- RAG 启动改为配置感知：未指定 Embedding 模型时不连接 Qdrant，显式设置 `QDRANT_ENABLED=false` 时不探测网络；系统健康页分别显示等待配置、已停用、不可用和维度不兼容。
- 新增 `scripts/check-rag-stack.sh` 只读部署自检，核验模型 ID、Embedding 实际维度、Qdrant collection 维度和三者一致性，不输出 Token；后端自动测试基线更新为 173 项。
- 补齐知识导入审核闭环：知识管理员拥有独立待审核队列，可查看其他导入人的非公开待审条目；审核结论单次锁定并回写导入项目/批次，批准后触发索引，驳回必须说明原因并禁止进入 RAG。
- 法规确认前强制完成官方文件暂存或人工补传，补传限制为受支持格式和 50MB；文件复制增加事务回滚清理，导入与审核写操作全部留审计记录。
- 法律法规、律所制度和授权参考资料在新建或修改后统一进入审核，禁止通过修改知识来源绕过审核；普通律师浏览器回归确认不显示来源同步/待审核入口，知识页无横向溢出和控制台错误。后端自动测试基线更新为 181 项，前端生产构建通过。
- 修复知识文章与工作汇报共用 `idx_created_at` 导致的 H2/PostgreSQL 全局索引名冲突；运行态重启确认 DDL 重复索引警告已消失。
- 完成员工首次登录强制改密闭环：员工导入、管理员新建及重置密码会设置 `must_change_password`；登录和当前用户接口返回该状态，后端在改密前以 `428` 阻断全部业务接口，修改成功后解除，且新密码不能与旧密码相同。
- 增加一次性存量账号迁移标记，当前 H2 运行态已标记 57 个普通员工；`admin/amin` 开发账号明确排除。普通律师“曾进朗”浏览器回归已自动进入个人中心并显示不可关闭的改密提示，页面无横向溢出、控制台无错误。
- 密码修改与管理员重置均不记录请求参数，防止密码进入审计日志；后端自动测试基线更新为 192 项，前端生产构建通过。
- PostgreSQL 备份改为 custom format，先写 `.part`，经 `pg_restore --list` 校验后原子落盘并记录 SHA-256；失败记录可持久化，路径越界、符号链接和物理删除失败不会误标为已删除。
- 系统设置新增备份完整性状态与手动校验入口；未校验或校验失败的备份不能从界面发起恢复。PostgreSQL 主库禁止应用内在线覆盖，只允许停机离线恢复。
- NAS 容器部署固定安装 PostgreSQL 16 客户端，并增加 `deploy/nas/backup-postgres.sh`、`restore-drill.sh` 和 180 天受控保留策略。脚本语法、后端 197 项测试和前端生产构建已通过；因当前 Mac 没有 Docker/PostgreSQL 客户端，真实 NAS/PostgreSQL 恢复演练仍待部署环境执行。
- 腾讯云路径进一步分档：脱敏、低依赖展示可用轻量 Linux；需要长期访问律所 NAS/模型机的混合云试用优先 CVM/VPC + VPN。Windows 云工作站不是当前 Java/容器栈的默认选择。
- 新增 `deploy/cloud` 可执行部署包：`DETACHED_DEMO` 强制脱敏且禁止 NAS/模型机连接，`HYBRID_VPN` 强制 NAS 挂载、VPN 健康检查和 RFC1918 模型地址；容器仅绑定回环地址，并可从已校验域名/端口自动渲染宿主机 Nginx HTTPS 配置。
- 云端 PostgreSQL/Qdrant 固定使用本机持久卷，案件/知识原件在混合云模式使用 NAS；数据库备份先在云端本地生成并校验，再原子复制到 NAS 异地目录。配置预检覆盖密钥、域名、文件权限、路径边界、公开端口和模式隔离。
- 修复 NAS 容器后端只有 `internal` 网络导致无法调用局域网模型或云端 AI 的问题：后端新增无端口映射的受控出站网络，PostgreSQL/Qdrant 仍只位于内部网络。
- `deploy/cloud/test-validate-config.sh`、全部部署 Shell 语法、NAS/云端 Compose YAML 与 `git diff --check` 已通过；当前 Mac 无 Docker，镜像构建、CVM/VPN/NAS 挂载和真实恢复仍需在目标 Linux 环境验收。
- 新增 H2 到 PostgreSQL 的离线三段式迁移工具：停机复制 H2、记录 SHA-256、先兼容演练/计划、目标库名二次确认后执行；目标必须为空，按外键顺序复制并保留主键，失败整体回滚，成功后校正 PostgreSQL 序列并写迁移审计。
- 使用当前 H2 停机副本完成兼容迁移实测：66 张逻辑表、11078 行数据全部迁移，逐表行数和主键 SHA-256 均一致；历史空 `WORK_REPORT` 大写重复表被安全忽略，有数据的 `work_report` 正常迁移。
- 独立目标结构生成器已复用应用命名策略和 Spring 管理的加密转换器，避免 `systemPrompt/system_prompt` 与手机号、邮箱字段在迁移建模时偏离正式应用；Java 入口会独立验证 H2 副本绝对路径、只读模式、非符号链接和实际 SHA-256。迁移专项 10 项、后端全量 207 项测试全部通过。
- 当前 Mac 的 PostgreSQL 16 安装环境不可用：`brew install postgresql@16` 已下载 bottle，但 Homebrew 在读取 bottle tab 时触发 `undefined method '[]' for nil`；`brew update` 又因 `/opt/homebrew` 无法执行 `git stash` 而失败。未重置或清理用户的 Homebrew 目录，因此尚未执行真实 PostgreSQL 目标库迁移、回滚、逻辑备份恢复和并发验收；该项继续保持 P0，不能以 H2 兼容演练替代。
- 收紧 AI 写入边界：通用聊天和案件聊天统一改为只读生成，旧版模型 `COMMAND` 标记不再触发任何业务写入；需要操作案件时只能走具有幂等键、动作白名单和风险确认的案件 AI 指令服务。
- 新案件 AI 指令只持久化“内容已移除 + 字符数”的元数据摘要和原文 SHA-256，不保存指令原文；AI 日志只保存脱敏摘要、输入/输出哈希、provider、模型、token、耗时、状态和估算费用。手机号、区号固话、身份证号、统一社会信用代码、邮箱和银行卡号均纳入日志摘要遮蔽。
- 案件 AI 工作台移除未实际参与规则解析的 provider 选择器，明确显示“规则引擎 / 本地识别”；知识问答和文书生成仍保留真实模型选择能力，云端模型不自动回退。
- AI 问答和文书草稿前端已移除 `v-html`，使用保留换行的纯文本渲染；聊天、案件指令和文书生成请求增加长度校验，防止无边界请求进入模型链路。
- 文书生成在读取案件信息或调用模型前执行案件对象级可见性检查，并统一使用 `AIGenerationGateway`；前端修正 JSON/Blob 协议错配，草稿可以安全预览、复制和下载为文本，不再把纯文本伪装成 Word 文件。
- 后端全量 215 项测试、前端生产构建、`git diff --check` 均通过；新增 AI 指令哈希字段后，使用同一停机 H2 副本重新干跑，66 张逻辑表、11078 行及主键摘要仍全部一致。
- 离线 H2 副本中已有 2 条 AI 指令和 2 条 AI 日志，可能保留旧版完整内容；本轮未在无备份确认的情况下改写历史数据。上线前须先备份并执行一次性脱敏迁移，旧记录的 `instruction_hash` 允许暂为空。
- 移除旧文书识别完成后自动建案、建待办、建日程和写工作日志的业务执行器；兼容性识别接口现在只进行本地文字/OCR提取和本地模型结构化识别，结果必须由用户核对，文件归案统一进入智能归案确认流程。
- AI 文书识别和法律要素提取统一使用 `AIGenerationGateway`，清理各服务内重复的 DeepSeek/OpenAI/Ollama 分支；智能文书识别固定调用 LM Studio，不允许自动回退云端。
- OCR/文书提取调用日志改为只记录操作类型、文书类型、格式和文本长度等元数据，不再把文书正文交给日志摘要；模型输出只保存哈希。
- 文书生成入口从固定 `ADMIN/LAWYER` 角色判断改为 `CASE_VIEW` 权限，实习律师、助理和其他获授权人员不再被角色名误拦；服务层仍在调用模型前校验案件对象权限。
- 审批创建接口会先校验关联案件可见性；审批处理、撤回、催办、列表过滤和详情访问的对象级拒绝统一使用权限异常并继续交给全局 `403` 处理。待归案文件跨用户读取同样返回权限拒绝。
- 旧 AI 侧栏移除自动匹配后创建待办和伪归档开关；手动创建待办增加确认，真实文件归案只允许从案件详情的智能归案流程完成。
- 审批创建、同意、驳回、转审和列表查询改用结构化请求 DTO：限制审批意见、标题、正文、附件、关键词、分页上限和排序白名单，保持现有 JSON 字段兼容。审批创建先校验关联案件可见性，服务层把越权、资源不存在和状态不允许分别映射为 403、404 和 400。
- 审批中心筛选从占位提示改为真实接口：审批类型使用后端代码值，状态、申请日期范围、标题/内容/申请人均可组合检索，并增加 20/50/100 服务端分页。已办页改为数据库状态组过滤后分页，修复“前 100 条多为待办时已办记录消失”的问题。
- 审批页面移除旧蓝色渐变、12px 圆角和悬浮位移动画，统一为 8px 内圆角、浅灰背景、白色工作区和克制系统蓝；表格补加载态、空状态和分页反馈。
- 新增审批筛选 JPA 数据库切片测试，真实执行状态组、日期边界、申请人姓名与审批对象可见性组合；测试内隔离无关 MyBatis Mapper，避免用纯 Mockito 代替查询语义。
- 后端全量 231 项测试、前端生产构建通过；当前浏览器中的账号均被首次改密策略退回登录页，真实行政/主任浏览器审批 E2E 尚未完成，不能由 JPA/服务测试替代。
- 通用 `/ocr/recognize` 接口移除模拟识别结果，统一复用本地文档提取链路：文字型 PDF、DOCX、TXT、MD 直接解析，图片通过 Tesseract，扫描 PDF 通过 pdftoppm 分页后再执行中文 OCR；临时文件始终清理，并限制支持格式、50MB 文件大小、100 页 PDF 和外部命令处理时限。
- OCR 健康接口不再固定返回“正常”，现在分别报告文字文档提取、图片 OCR、扫描 PDF OCR、配置语言包和 `READY/DEGRADED` 状态；识别接口要求登录、记录无正文审计日志，并对调用方隐藏本地命令和内部路径错误。
- 使用仓库已有中文进度汇报 PDF 的页面完成本机 `pdftoppm + Tesseract chi_sim+eng` 命令级真实识别，能够识别系统名称、报告标题和日期，原 PDF 未修改。当前 `/Volumes/scan` 未挂载，因此传票、判决书、裁定书、保全文书和扫描图片样本仍待只读验证。
- 后端自动测试基线更新为 240 项全部通过，前端生产构建通过；新增 OCR 服务、控制器、文字层 PDF、页数上限、命令与语言包健康状态、安全和审计覆盖。真实 NAS/Linux 容器 OCR、浏览器上传以及四类案件角色 E2E 仍未完成。
- 新 JAR 已重新加载到当前局域网测试环境，后端 `/api/health` 与前端均返回 HTTP 200；未登录访问 `/api/ocr/health` 返回 401，确认运行态仍执行接口鉴权。认证后的 OCR 能力状态及文件上传仍待真实角色浏览器验收。
- 管理员系统健康中心已接入 OCR：统一详情接口返回文字文档、图片、扫描 PDF、语言和说明，扫描 PDF 能力不可用时整体状态转为 `degraded`；前端运行状态页分别展示三项能力，不再要求管理员通过终端判断命令和语言包。后端自动测试基线更新为 242 项，前端生产构建通过。
- 核心左侧导航补齐律师“日程管理”入口以及案件“归档案件/回收站”独立子页面，创建页、利冲、财务、AI 与知识子页按真实路由高亮；行政和财务继续隐藏律师日历。系统设置深链标签与实际 `scope/logs/backup` 页面统一，修复 `?tab=backup` 等地址无法直达的问题。
- 共享页头改为无浮动卡片的浅色分隔层级；全局搜索移除固定 400px 输入宽度，支持手机换行、URL 关键词变化和卸载时取消延迟搜索。脱敏浏览器在 1440×900 律师工作台、1024×768 管理员备份页和 390×844 搜索/移动侧栏完成渲染：页面横向溢出均为零、无 JavaScript 页面错误，新建案件菜单正确展开并高亮。
- 客户列表改为服务端统一组合筛选：客户名称、案源人、承办人、类型和部门均在权限过滤后、分页前执行，筛选总数不再沿用未过滤总数；分页控件重新接通请求。普通员工可查看本部门案源人或承办人关联客户，但不能借同部门关系取得编辑/删除权限；全库查看继续由 `CLIENT_VIEW_ALL` 控制。
- 案件列表补齐案件名称/案号、案由、部门、主办律师、审理机构和立案日期组合筛选，后端校验日期格式与先后关系；仲裁机构允许自由填写。批量操作根据案件状态禁用不适用动作，批量归档会保存档案保管地，批量结案/归档/变更主办人成功后不再调用不存在的刷新函数。
- 案件与客户列表统一使用克制浅色筛选区和共享数据表，移动端案件高级筛选默认收起，固定操作列和分页保持可用；当前模块内隐藏无权限的新建、编辑、归档、删除与批量选择，桌面顶栏不再与当前业务页重复显示相同新建命令。
- 脱敏浏览器已验证案件 1440×900/390×844、客户 1024×768/390×844，四个视口均无页面横向溢出和 JavaScript 错误；第一页与第二页分别发送 `page=0`、`page=1`。只读账号检查中案件/客户写按钮与案件批量选择均为零。后端全量 248 项测试、前端生产构建及 `git diff --check` 通过；真实员工角色浏览器 E2E 仍是 P0。
- 案件详情改为使用后端返回的有效操作权限，编辑、阶段变更、删除与归档不再仅依据前端全局权限；已结案/已归档案件锁定编辑，单案归档只允许已结案状态并强制填写档案保管地。详情路由参数变化会重新加载，失败时提供明确重试和返回入口。
- 案件基本信息页按移动端自适应描述列和弹窗宽度；只读账号不再看到团队、收费、程序和策略维护入口。移除未落库的“添加关联案件”、策略评论输入及可误导用户的程序附件删除控件。
- 客户详情恢复七类真实客户类型，完整展示客户关系、角色、所属部门、案源人、承办人、主体和联系方式；关联案件同时兼容结构化客户 ID 与历史当事人名称，并按当前账号案件对象权限过滤，防止通过客户详情枚举无权案件。
- 客户关联案件和沟通记录修复直接数组响应解析；沟通记录统一使用后端 DTO 字段，补齐沟通日期校验与操作人姓名。新增/修改/删除沟通记录均要求后端 `CLIENT_EDIT`，页面按钮同时按客户关系与权限隐藏。
- 后端全量 253 项测试、前端生产构建和 `git diff --check` 通过；新 JAR 已加载，`/api/health` 返回后端与数据库 `ready`。案件/客户详情真实桌面及移动端浏览器复验因当前环境未提供测试密码而保持未完成，未通过改库或伪造登录绕过。
- 案件列表接通后端逐案有效操作权限：编辑、删除、归档按钮不再只看账号全局权限，批量复选框仅允许选择至少具备一项有效写操作的案件，各批量命令再次校验所选案件的逐案能力和状态。删除、恢复、单案/批量归档的服务入口同步增加对象管理范围校验，行政只读账号不能通过直接调用接口越权。
- 修复案件列表“下次开庭”前后端字段不一致：后端从案件 `hearingDate` 统一映射 `nextHearingDate`，列表和本页看板兼容读取并正确显示。共享 `DataTable` 新增可配置逐行复选资格，不影响其他列表默认行为。
- 后端全量测试基线更新为 254 项，覆盖本人案件与无关案件在同一响应中的不同有效操作集合、行政管理范围拒绝和开庭日期映射；前端生产构建及 `git diff --check` 通过。
- 系统设置新增“隐私治理”页和 `GET /api/system/ai-privacy/preview`、`POST /api/system/ai-privacy/cleanup`。管理员可预览旧 AI 日志/指令数量；只有选择时间覆盖全部待处理记录且完整性校验通过的数据库备份，并输入固定确认词后，才可事务性清除旧输入/输出原文、替换指令内容并补齐哈希和逐记录脱敏时间。
- 新案件 AI 指令进一步收紧为纯元数据摘要，短指令也不再近似原文落库；AI 错误日志不保存异常正文。清理接口不记录参数或响应正文，引用的清理前备份会标记为含历史敏感原文。当前 H2 历史数据尚未执行破坏性清理，需管理员在页面核对备份后操作。
- 修复系统设置 `?tab=privacy` 等深链首次进入不加载对应数据的问题。后端全量 258 项测试、前端生产构建和 `git diff --check` 通过。
- 知识文档上传统一接入本地文档提取服务：用户可直接上传 PDF，文字型 PDF 自动提取正文，扫描 PDF 自动通过本机 `pdftoppm + Tesseract` OCR；原件继续保留，不把文档发送云端。
- 国家法律法规数据库导入增加公开详情/附件适配器、50MB 限制和 11 项常用法律及司法解释基础清单；官方文件改为 DOCX 优先、PDF 回退，同时校验下载域名、文件签名与大小。已经生成的待审 PDF 条目可在批准前用官方 DOCX 安全刷新正文和附件，失败时保留原待审条目，不产生半成品。
- 修复 `admin` 开发管理账号缺少 `KNOWLEDGE_MANAGE` 的权限映射，已在登录态浏览器中验证可见“来源同步”和待审核操作。普通律师仍不具备该权限。
- 2026-07-23 已在当前 H2 测试库实际执行基础法规批次：民法典、民事诉讼法、刑法、刑事诉讼法、行政诉讼法、行政复议法、仲裁法、公司法、劳动合同法和律师法共 10 部法律经官方文件与正文人工核验后批准发布。《最高人民法院关于适用〈中华人民共和国民事诉讼法〉的解释》因官网未返回可下载附件，保持“需补传”；没有使用非官方文件替代。
- NAS 扫描出的《广东至高律师事务所章程、合伙协议》当前 OCR 摘要质量不达标，仍保持待审核且禁止索引，未因本轮法规导入而误批准。
- 本轮后端全量 262 项测试、前端生产构建通过。新增覆盖待审 PDF 用官方 DOCX 刷新及 `admin` 知识管理权限；系统仍保持局域网测试态运行。
- 立案审批收费摘要改为读取案件登记明细：固定收费显示代理费金额，风险收费显示金额或比例，组合收费显示“固定收费 + 风险收费”。新建的行政初审和主任终审会写入完整摘要，存量待审批单在读取时按关联案件最新收费数据替换旧的收费简称。
- 修复审批中心页码被全局拦截器误减一、向后端发送 `page=0` 导致整页加载失败的问题。审批 API 现明确保留一基页码，不改变其他历史零基分页接口。
- 浏览器已以 `admin` 实际回归当前顾问案件审批，审批中心恢复 3 条待办，“佛山市洛伽建材有限公司常年法律顾问”详情显示“收费方式：固定收费 10000元”。后端全量 266 项测试、前端生产构建和 `git diff --check` 通过，重启后健康状态为 `ready`。
- 统一案件“立案时间”口径：新建申请时后端强制为空，普通案件编辑不再接受该字段，主任终审通过时以该审批单 `approvedTime` 的日期覆盖写入。待审批页面显示“审批通过后生成”，案件编辑弹窗移除手工立案日控件。
- 案件详情接口新增主委托客户名称，优先根据关联客户 ID 获取，并兼容顾问单位和历史委托当事人。前端已移除“有委托客户/查看客户详情”，改为在案件信息中直接展示名称。
- 浏览器实际回归案件 9：委托客户显示“佛山市洛伽建材有限公司”，立案时间显示“审批通过后生成”。未为验证而审批真实案件；终审日期写入通过后端测试验证。后端全量仍为 266 项通过，前端生产构建通过。
- 案件基本案情的代理律师费改用统一收费方式字典，`FIXED`、`CONTINGENT`、`BASE_PLUS_CONTINGENT`、`OTHER` 分别显示为“固定收费”“风险收费”“固定+风险”“其他”，并兼容历史收费代码；详情编辑选项同步收敛为新建立案的四类。浏览器已实际回归案件 9，页面显示“收费方式：固定收费”，前端生产构建通过。

### 2026-07-24

- 明确 GD、AC、SSB 的最终融合标准：理解并复现原项目业务规则与算法，统一使用 ZGAI 界面、账号、权限、案件、客户、文件、状态和审计；外部跳转、独立 API 页面、第二套账号或案件数据库均不算融合完成。
- 建立 ZGAI 原生民事智能归档任务：仅已结案民事案件可发起，流程覆盖本地 OCR/分类/提取、律师核对、材料补传、行政复核、驳回理由、同源预览、合卷和成功后状态锁定。
- 新增 `archive-worker` 私有计算组件，将 GD 的民事目录、字段提取和 PDF 合卷逻辑整理为 ZGAI 内部能力；Worker 无独立用户、案件或业务数据库，只能读取配置允许的案件根目录，固定使用本地 LM Studio，不回退云端。
- 固定 PDF 表格引擎完成 A4 版式、内嵌中文字体、目录书签、可搜索 OCR 文本层和图片等比无裁切；最终 PDF 与 JSON 来源清单按版本写入 `99_归档材料`，记录 SHA-256、页码区间与页守恒结果。
- 停用旧单案/批量直接归档服务，防止绕过律师核对、行政复核和文件校验直接设置 `ARCHIVED`；行政新增 `CASE_ARCHIVE_REVIEW` 权限，驳回任务可由原律师修改后重新提交。
- 已归档案件可填写更正原因发起新任务，旧电子卷宗和来源清单不会被并入新卷；更正仍经过律师核对和行政复核，生成 v2 并保留 v1。
- NAS Compose 已增加不公开端口的归档 Worker、独立 OCR 缓存、内部强令牌和受控模型机出站网络。归档 Worker 2 项真实生成测试通过；NAS 容器构建、脱敏民事卷宗、真实律师/行政浏览器闭环和 500 页压力测试仍待完成。
- 公章用印审批升级为真实文件流程：审批中心可上传文件，系统按 `SEAL_APPROVE` 自动路由行政；审批抽屉展示并下载附件，同意意见和驳回理由留痕，结果同步附件状态、案件进展和申请人通知。
- 案件文档列表增加“申请用印”，直接引用原 `case_document` 且拒绝跨案件引用；顾问案件增加“上传法律意见书”快捷入口，继续使用案件目录和版本管理。NAS/云端部署新增独立 `APPROVAL_FILE_ROOT` 持久化卷。
- 修复首次改密过滤器白名单中的 `/users/change-password` 路径；隔离账号验证改密前业务接口返回 `428`，用户控制器改密成功后立即解除限制。
- 客户、案件和发票对象级越权统一返回 `403`；隔离跨部门律师验证客户详情、案件详情和案件文件上传均被拒绝。新增对应服务测试，避免控制器把访问拒绝吞成通用 `500`。
- 修复发票反馈的 ISO 日期表单绑定和相对路径文件保存缺陷，改为统一绝对上传根目录并阻止路径越界。隔离实例完成申请、待审修订、普通律师处理拒绝、财务上传、申请人下载、完成锁定和完成后禁止删除，反馈文件 SHA-256 与上传原件一致。
- 隔离 H2 实例使用普通律师、行政、主任、财务和跨部门律师五类虚构账号跑通客户创建、立案利冲、行政初审、主任终审、正式案号与审批日生成、一案一档、利冲报告归档、案件文书上传和快速用印；用印附件下载哈希一致，申请人自审被拒绝。
- 真实浏览器先以隔离普通律师账号验证工作台、案件详情、客户名称、立案日期、收费中文显示、利冲归档和类型化办理重点，无页面横向溢出；随后补齐行政、主任和财务独立账号的工作台、审批与开票页面验收，详见本周末尾最新基线。
- 对当前全部新增和优化功能重新执行集成基线校验：后端 285 项测试、前端生产构建、归档 Worker 2 项 `unittest`、全部 Shell 语法、腾讯云安全配置反例、四角色和管理员冒烟、`git diff --check` 均通过。
- 清理仓库中 24 份历史后端运行日志、5 份早期测试案件归档 PDF、2 份与正式旧案导入模板 SHA-256 完全一致的 Excel 副本，以及已被 README/PRD/Handoff 取代的夜间临时指令。正式模板、NAS/AI 部署预算方案和有效自动测试全部保留。
- 本轮使用的是隔离虚构账号，不是律所真实员工账号；也未在目标 NAS 构建 Compose。真实角色浏览器与 NAS 容器验收仍不得由本轮结果替代。
- 修复本机停止脚本直接使用 `kill -9`、跳过 Spring 资源释放的问题：`stop.sh` 现支持按 `backend/frontend/ssb/ac` 选择服务、环境变量覆盖端口和停机超时，先发送 `SIGTERM`，仅在超时后强制终止。隔离 H2 后端已分别通过直接 `SIGTERM` 和新版脚本停机实测，JPA 与 Hikari 均正常关闭，原 `8080/3017` 演示实例未受影响。
- 再次扫描 Git 跟踪文件后，未发现新增日志、测试输出、临时备份或可确认失效的阶段报告；AC 子项目设计文档、部署说明、预算方案和有效测试资产均有后续融合或运维用途，本轮不作误删。
- 角色管理移除前端模拟权限树、无效状态/创建时间字段和未定义选中变量，修正一基页码误传导致首页跳过；新增后端最小权限目录，系统角色明确显示为身份权限基线维护且禁止页面写入，自定义角色可从当前真实权限目录分组勾选。
- 修复认证响应把 `ROLE_VIEW/ROLE_EDIT` 权限误识别为 `VIEW/EDIT` 角色的问题：现在角色只来自有效用户角色关联，`ROLE_USER` 不对外展示，权限名即使以 `ROLE_` 开头仍保留在权限集合。行政 OA 新增 `USER_VIEW` 路由门槛，角色页签额外要求 `ROLE_VIEW`。
- 隔离浏览器使用虚构管理员创建 `BROWSER_QA_2026` 自定义角色并保存 `CASE_VIEW + CLIENT_VIEW`；虚构普通律师首次改密前访问业务接口为 `428`，改密后权限目录为 `403`，直接访问 `/admin-oa` 被退回律师工作台。管理员登录响应为真实 `ADMIN` 角色且角色页签可见，律师响应为真实 `LAWYER` 角色。
- 定位停机类加载异常的触发条件为“运行中的 Spring Boot 可执行 JAR 被 `mvn package` 原地覆盖”。`start.sh` 现统一调用 `stop.sh all` 优雅停止旧服务后再构建，README 明确禁止覆盖运行中的 JAR；正常未覆盖实例仍可优雅关闭。
- 本轮后端全量测试增至 287 项，前端生产构建、归档 Worker 2 项、全部 Shell 语法、云部署安全配置反例和 `git diff --check` 均通过。
- 员工目录完成隐私分层：完整用户列表和详情改为 `USER_VIEW`，新 `/users/options` 仅向已登录业务页面返回在职人员 ID、姓名、部门和身份类别；案件新建/筛选/详情、客户新建、审批发起和日程表单均已迁移，不再为下拉框下载手机号、邮箱、账号、角色和登录时间。
- 隔离管理员与虚构普通律师完成 API 和浏览器复验：律师访问完整列表/详情均为 `403`，最小目录不含敏感键；客户案源人及案件主办下拉均可看到有权使用的在职员工，直接访问行政 OA 继续退回工作台，浏览器控制台无错误。
- 用户管理页同步修复一基页码、字符串角色误渲染、空日期显示 `Invalid Date` 和开发账号无效操作按钮；`admin/amin` 现在显示“受保护开发账号”，服务端拒绝通过员工管理修改资料、停用、删除或调整角色。其他具备权限的管理员仍可执行密码恢复，且恢复后开发账号不被首次改密策略锁住；当前账号不能重置自己的密码。
- 清理 `backend/` 根目录 30 个无任何代码/脚本引用的早期手工 `curl` 请求与响应 JSON；正式自动测试 fixture、GD/AC/SSB 规则资料、部署与预算文档全部保留。本轮后端全量测试基线更新为 289 项。
- 统一工作台和审批中心的身份识别：主任同时认可 `MANAGER` 角色与职位，行政和财务统一读取角色代码，避免职位文字变化导致页面或审批范围缺失；主任审批中心使用独立标题和管理说明。
- 开票待办分派由单一 `INVOICE_PROCESSOR` 偏好扩展为“专职开票处理人 → `FINANCE` → 其他持权账号”，防止未配置专岗时待办落到开发管理员；开票日期数组统一格式化为 `YYYY-MM-DD`。
- 隔离 H2 新建律师、行政、职位为“部门负责人”的 `MANAGER` 主任和财务账号完成 API 与浏览器复验：行政待办卡片、审批表格和详情抽屉同步显示用印附件及同意/驳回入口；律师越权审批和发票反馈均为 `403`；财务完成反馈与锁定后，律师仅看到“反馈文件”；主任可查看全局审批。
- 删除仓库根目录 13 个无引用、字段过时的手工业务请求 JSON 和 1 个占位云端 AI 配置 JSON；保留自动测试 fixture、GD/AC/SSB 规则资料、部署说明与预算文档。
- 全量回归更新为后端 291 项、前端生产构建、归档 Worker 2 项和全部 Shell 语法通过；前端仍存在 Sass legacy API 与大分块非阻断警告。
- 新增带显式写入确认保护的 `scripts/e2e-approval-finance.sh`，在隔离 H2 中以律师、行政、`MANAGER` 主任和财务四类虚构账号重复跑通用印与开票闭环：律师及无权账号越权处理均为 `403`，主任可全局查看但不抢占当前审批人，行政完成用印，财务上传并锁定发票，申请人下载反馈文件的 SHA-256 与原件一致。
- 隔离桌面浏览器复验行政审批中心和财务发票页：行政可在审批抽屉查看附件并执行驳回/同意用印，财务可见待审查与已完成记录并打开包含拖拽上传框的“开票反馈”弹窗。审批筛选、角色工作台、表格、分页和财务弹窗补齐窄屏响应规则；尚未完成行政、主任、财务 390px 浏览器截图，因此移动端验收继续保留为 P0。
- 本轮再次扫描 Git 工作区和已跟踪资料，未发现新增运行日志、手工响应、缓存、重复 fixture 或可确认失效的阶段报告；README、PRD、Handoff 继续作为唯一长期进度文档，不为本轮创建额外总结文件。
- 删除无人引用且无法执行的 Maven Wrapper：其中 `maven-wrapper.jar` 实际是 554 字节 HTML 错误页；后端继续使用 README 约定的 Java 11 与系统 Maven。完整第三方 Maven 源码目录未与测试垃圾混同，本轮不做 2049 文件的大规模历史删除。

## 4. 已完成能力

以下能力已有当前代码与至少一种测试/构建依据：

- 用户、部门、身份类别和权限集合。
- 真实角色/权限响应拆分、系统角色保护、自定义角色真实权限目录与行政页面路由隔离。
- 案件与客户部门数据范围。
- 客户全库利冲检查、检查留痕、行政正式结论、复核人/时间，以及文本与原件豁免依据。
- 立案审批中心、详情抽屉、行政/主任处理基础、驳回理由和转审；公章用印真实附件、行政路由、案件快捷申请与结果留痕。
- 案件文件目录、NAS 存储、元数据、版本、回收站和分片上传。
- 发票申请、反馈文件和完成锁定。
- 法规/制度/模板/授权参考知识库与关键词降级检索。
- 元典法律数据检索、引证核验和受控知识导入（真实调用待配置 API Key）。
- LM Studio 本地生成模型接入，覆盖 AI 问答、RAG 回答、文书草稿和结构化提取。
- AI 新日志/指令的无原文最小化留痕、受控历史脱敏、聊天只读边界、模型输出纯文本渲染和文书生成案件对象级权限。
- 旧案资料安全检索与受控下载。
- 系统健康、审计、可校验 PostgreSQL 备份、受控保留和离线恢复演练脚本。
- H2 离线迁移、空目标保护、主键/行数校验、序列校正和迁移审计工具；真实 PostgreSQL 16 实机执行仍待验收。
- 案件 AI 工作台、低风险指令自动执行、高风险确认、结构化进展和智能文件归案。
- 民事智能一键归档的 ZGAI 原生任务、私有 Worker、材料/字段核对、行政复核、同源预览、来源清单和成功后状态锁定。

“已完成”仅表示达到当前代码基线，不代表生产验收。真实角色 E2E、PostgreSQL 迁移、NAS 稳定性和灾备恢复仍是上线前置条件。

## 5. 当前高优先级缺口

### P0

1. 使用普通律师、行政、主任、财务真实账号跑完整权限回归。
2. 在 PostgreSQL 16 测试环境执行已通过兼容演练的 H2 数据迁移，并完成回滚、备份恢复和多人并发演练。
3. 使用浏览器自动化覆盖客户、立案、两级审批、建档、文件和发票闭环。
4. 用脱敏传票、判决书、裁定书、保全文书和扫描图片验证智能归案，覆盖模型离线、OCR 失败、NAS 断开和跨部门权限。
5. 补齐行政、主任和财务移动端审批/开票页面验收；桌面隔离角色浏览器回归已完成。
6. 在 NAS 容器构建归档 Worker，使用脱敏民事卷宗完成律师发起、材料核对、行政批准、PDF 下载 E2E，并验证 500 页、模型离线、NAS 断开和 LibreOffice 失败不产生半成品归档状态。

### P1

1. 真实 NAS 断网、重连、并发、大文件和权限测试。
2. 评估有合法授权的工商数据源后，再扩展利冲多层股权与实际控制链穿透；一层显式关系已完成。
3. 使用真实官方链接和 NAS 首批制度运行已实现的导入流程，核验 403 人工补传、DOC 转换、审核发布和 RAG 准入。
4. 在模型机加载并核验本地 Embedding 模型，在 NAS SSD 启动持久化 Qdrant 后跑 RAG 评价集；若 NAS 压测不达标，再评估将 Qdrant 迁至模型机本地 NVMe。
5. 配置正式元典 API Key，完成真实调用与至少 20 题质量、成本和授权评测；再抽象 `LegalSourceProvider` 并与北大法宝对照。
6. 用户提供旧资料样本后建立离线索引和性能基线。
7. 由管理员在“系统设置 → 隐私治理”对当前 H2 历史 AI 记录执行一次受控脱敏，并登记所选备份的保留/销毁决定；开发过程不得代替管理员执行。

## 6. 推荐并行工作流

每个工作流使用独立分支和 worktree。分支统一使用 `codex/` 前缀。

| 工作流 | 推荐分支 | 主要目录 | 交付物 |
|---|---|---|---|
| A 案件与审批 | `codex/case-approval` | `controller/Case*`、`service/Case*`、`Approval*`、案件前端 | 两级审批 E2E、审批规则配置化 |
| B 客户与利冲 | `codex/client-conflict` | `Client*`、`Conflict*`、客户前端 | 主体关系、利冲报告、权限回归 |
| C 文档与数据库 | `codex/storage-postgres` | `CaseDocument*`、`Storage*`、SQL、备份脚本 | PostgreSQL 迁移、NAS/灾备测试 |
| D 知识与 AI | `codex/knowledge-rag` | `Knowledge*`、`RAG*`、`Qdrant*`、法律数据适配器、知识前端 | 内容导入、供应商对照评价、模型接入 |
| E 前端设计 | `codex/ui-system` | `frontend/src/layouts`、样式、共享组件 | 设计 token、四类工作台和响应式页面 |
| F QA 与安全 | `codex/qa-security` | `backend/src/test`、E2E、`scripts/` | 角色矩阵、浏览器回归、漏洞与压测报告 |
| G 项目能力内化 | `codex/capability-integration` | `archive-worker/`、后续 `ac-calc/`、`ssb/` 与对应 ZGAI 业务模块 | 把 GD/AC/SSB 规则迁入 ZGAI，不交付独立产品跳转 |

## 7. 文件所有权与冲突控制

以下文件是共享热点，同一时间只能由一个工作流修改：

- `backend/src/main/java/com/lawfirm/security/SecurityConfig.java`
- `backend/src/main/java/com/lawfirm/security/UserAuthorityService.java`
- `backend/src/main/java/com/lawfirm/config/DataInitializer.java`
- `backend/src/main/resources/application*.yml`
- `backend/src/main/resources/db/postgres/001_foundation_schema.sql`
- `frontend/src/router/index.js`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/stores/user.js`
- `PRD.md`、`handoff.md`、`README.md`

修改共享热点前，Agent 必须在任务说明中声明；若另一分支正在修改，由集成人员统一处理，禁止复制覆盖。

跨模块契约要求：

- 新增权限码时同步后端权限矩阵、前端菜单/路由和角色测试。
- 新增数据库字段时同步实体、DTO、SQL、映射层、前端和迁移验证。
- 修改状态机时同步枚举、服务、显示文本、审计、通知和测试。
- 新增文件下载时必须复用业务权限，并校验根目录与真实路径。
- 新增 AI 数据源时必须明确公开范围、授权、时效和是否可索引。
- 商业法律库只能使用供应商正式 API/MCP 和企业凭据；网页会员、人工下载权限不得用于抓取、整库复制或全文向量化。

## 8. 分支与合并流程

1. 从最新 `master` 创建 `codex/<scope>` 分支和独立 worktree。
2. 一个分支只处理一个工作流，不混入无关重构。
3. 提交前运行本模块测试、后端全量测试和前端构建。
4. PR 描述列出数据库变更、权限影响、配置项、验证结果和回滚方式。
5. 合并顺序：数据模型/权限契约优先，业务服务其次，前端最后，QA 回归收尾。
6. 合并后由集成人员更新本文周进度、测试基线和剩余缺口。

禁止：

- 多个 Agent 直接在同一工作目录同时修改。
- 未同步主分支就覆盖共享热点。
- 用前端按钮隐藏代替后端权限。
- 提交真实密码、AI 密钥、客户数据或 NAS 凭据。
- 根据旧报告宣称功能已完成。
- 把 H2 测试成功当作 PostgreSQL 迁移成功。

## 9. 验证清单

### 每个 PR 必跑

```bash
cd backend
JAVA_HOME=/opt/homebrew/opt/openjdk@11 /opt/homebrew/opt/maven/bin/mvn test
```

```bash
cd frontend
npm run build
```

### 权限或流程变更

```bash
./scripts/smoke-test-core.sh
./scripts/smoke-test-roles.sh
```

### 知识与 AI 变更

```bash
./scripts/evaluate-knowledge-rag.sh
```

必须人工检查：

- 未登录返回 `401`。
- 无权限返回 `403` 或统一业务拒绝，不泄露对象存在性和详情。
- 普通律师看不到其他部门案件/客户。
- 利冲可全库命中但不能打开无权客户。
- 行政能看到审批资料并明确操作，主任能终审。
- 文件和备份接口不返回物理绝对路径。
- 案件材料不进入知识库或 RAG。

## 10. 数据与环境注意事项

- 当前局域网测试数据可能位于 H2 文件库，切换 PostgreSQL 前先备份。
- NAS 挂载点不能写死在业务代码，必须使用配置项。
- 测试旧资料扫描时只使用只读目录和脱敏样本。
- 不删除或重置用户已有数据，除非有明确备份和书面确认。
- `admin`/`amin` 保护逻辑用于开发管理，正式生产应迁移为受控超级管理员机制。

## 11. 等待用户输入的事项

- NAS 品牌型号、CPU 架构、内存、存储池、SSD/HDD、Container Manager 和 UPS 情况。
- 四类真实测试账号的安全提供方式（暂缓）。
- 旧案资料根目录、脱敏样本和命名规则；行政人员填好的首批导入模板。
- 首批律所内部制度与公共模板文件。
- 具有 `KNOWLEDGE_MANAGE` 权限的管理员重新登录后，执行“知识库 → 来源同步 → 加载基础法规”，确认生成待审核知识并完成首批审核。
- 国家法律法规数据库只能通过公开且允许的方式低频增量采集；不得绕过验证码或构建未经允许的整站镜像。
- 律所制度源为 `smb://192.168.1.238/律所共享资料/律所制度、章程`，仅允许只读扫描和复制到受控暂存区，不得改动原文件。
- 模型机生成模型量化等级、上下文上限、首 token 延迟和吞吐基准。
- 生产域名、HTTPS、公网访问和日志保留策略（暂缓）。

Agent 遇到上述事项应暂停对应分支，不应自行猜测；仍可继续不依赖该决策的测试、权限、数据模型和 UI 工作。

## 12. 交接完成标准

Agent 完成任务时必须提供：

- 变更文件与业务效果。
- 权限和数据范围影响。
- 数据库/配置变更与回滚方法。
- 自动测试和人工验证结果。
- 未验证风险与下一步。
- 对 `handoff.md` 的最小更新。

没有上述证据的“完成”只视为开发中，不进入主分支完成清单。
